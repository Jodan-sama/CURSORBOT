# Kalshi (Key ID + RSA private key for request signing)
KALSHI_BASE_URL=https://api.trade.kalshi.com/trade-api/v2
KALSHI_KEY_ID=
KALSHI_PRIVATE_KEY=
# Private key: full PEM. In .env use \n for newlines, e.g.:
# KALSHI_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nMIIEow...\n-----END RSA PRIVATE KEY-----"

# Binance (public price API - no key needed for ticker/price)
# BINANCE_BASE_URL=https://api.binance.com

# Supabase (for positions log + bot config)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Polymarket (Gamma = read-only; CLOB = orders via proxy)
# Proxy Empire: use one session per process. Format username:password@host:port
# HTTP_PROXY=http://r_xxx-country-dk-city-copenhagen-sid-XXXX:759cf83569@v2.proxyempire.io:5000
# HTTPS_PROXY=<same as HTTP_PROXY>
POLYMARKET_PRIVATE_KEY=       # Wallet private key (hex, with or without 0x)
POLYMARKET_FUNDER=            # Wallet address (Polymarket profile)
POLYMARKET_API_KEY=
POLYMARKET_API_SECRET=
POLYMARKET_API_PASSPHRASE=

# Optional: Polygon RPC for CLOB/signer (e.g. Alchemy)
# POLYGON_RPC_URL=https://polygon-mainnet.g.alchemy.com/v2/YOUR_KEY
